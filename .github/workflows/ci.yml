name: CI
on:
  # main is covered by deploy.yml, which runs the same checks before deploying.
  push:
    branches-ignore: [main]
  pull_request:
  workflow_dispatch:
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true
jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Before setup-node: its yarn cache probe must resolve Yarn 4 via the
      # corepack shim, not the runner's global Yarn 1.
      - run: corepack enable
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: yarn
          cache-dependency-path: |
            yarn.lock
            site/yarn.lock
      - run: yarn install --immutable
      - run: yarn install --immutable
        working-directory: site
      # typecheck-all + test + pipeline + check-data-drift
      - run: yarn verify
      - run: yarn build
        working-directory: site
        env:
          BASE_PATH: /${{ github.event.repository.name }}/
